# NeoTermOS 🐧

NeoTermOS adalah sistem terminal berbasis Python
yang berjalan di Termux, dengan konsep mini-OS,
multi-user, virtual filesystem, dan package manager sendiri.

## ✨ Features
- Login system (root & user)
- Custom shell (# / $)
- Virtual File System
- NeoPkg package manager
- Modular architecture
- No fake SSH

## 📦 Install (Termux)
```bash
pkg install git python -y
git clone https://github.com/USERNAME/NeoTermOS
cd NeoTermOS
python main.py
